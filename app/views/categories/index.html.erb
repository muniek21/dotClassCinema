<p>Find me in app/views/categories/index.html.erb</p>

<h1>Categories#index</h1>
<% @categories.each do |category| %>
	<div>
	<%= link_to category.name, category %> (<%= category.movies.count %>)
	<%= image_tag category.image_url %>
	</div>
<% end %>
